{
    "title": "Continental Club Austin Calendar Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "maxEvents": {
            "title": "Maximum Events to Scrape",
            "type": "integer",
            "description": "Maximum number of events to scrape (0 = unlimited)",
            "default": 0,
            "minimum": 0
        },
        "dateRange": {
            "title": "Date Range Filter",
            "type": "string",
            "description": "Filter events by date range (upcoming, this-month, next-month, all)",
            "default": "upcoming",
            "enum": ["upcoming", "this-month", "next-month", "all"],
            "enumTitles": ["Upcoming Events", "This Month", "Next Month", "All Events"]
        },
        "includeNonConcerts": {
            "title": "Include Non-Concert Events",
            "type": "boolean",
            "description": "Whether to include non-concert events like bingo, trivia, etc.",
            "default": false
        }
    },
    "required": []
}