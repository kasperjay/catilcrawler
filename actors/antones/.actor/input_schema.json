{
  "title": "Antone's Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Calendar URL to scrape events from",
      "default": "https://antonesnightclub.com/calendar/",
      "prefill": "https://antonesnightclub.com/calendar/",
      "editor": "textfield"
    },
    "maxEvents": {
      "title": "Max Events",
      "type": "integer",
      "description": "Maximum number of artist records to collect (0 = unlimited)",
      "default": 100,
      "minimum": 0
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum parallel pages (keep at 1 for calendar JS rendering)",
      "default": 1,
      "minimum": 1
    },
    "requestHandlerTimeoutSecs": {
      "title": "Request Handler Timeout (secs)",
      "type": "integer",
      "description": "Seconds before a page processing is aborted",
      "default": 120,
      "minimum": 30
    }
  },
  "required": ["startUrl"]
}
