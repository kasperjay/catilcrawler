{
  "title": "3TEN Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "ACL Live at 3TEN events page URL",
      "default": "https://www.acllive.com/events/venue/acl-live-at-3ten",
      "prefill": "https://www.acllive.com/events/venue/acl-live-at-3ten",
      "editor": "textfield"
    },
    "maxEvents": {
      "title": "Max Events",
      "type": "integer",
      "description": "Maximum number of events to collect (0 = unlimited)",
      "default": 500,
      "minimum": 0
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum parallel pages (lower reduces timeouts and memory)",
      "default": 3,
      "minimum": 1
    },
    "requestHandlerTimeoutSecs": {
      "title": "Request Handler Timeout (secs)",
      "type": "integer",
      "description": "Seconds before a page processing is aborted",
      "default": 120,
      "minimum": 30
    }
  },
  "required": ["startUrl"]
}
