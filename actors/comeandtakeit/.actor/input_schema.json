{
    "title": "Come & Take It Productions calendar scraper input",
    "description": "Configure the Come And Take It Productions calendar URL and crawling options.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
      "calendarUrl": {
        "title": "Calendar URL",
        "type": "string",
        "description": "URL of the Come And Take It Productions calendar page.",
        "default": "https://comeandtakeitproductions.com/calendar/",
        "editor": "textfield"
      },
      "maxConcurrency": {
        "title": "Max concurrency",
        "type": "integer",
        "description": "Maximum number of concurrent browser pages (higher = faster, more resource-hungry).",
        "default": 3,
        "minimum": 1,
        "maximum": 20,
        "editor": "number"
      }
    },
    "required": ["calendarUrl"]
  }
  