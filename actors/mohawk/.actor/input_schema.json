{
  "title": "Mohawk Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Homepage URL to scrape events from",
      "default": "https://mohawkaustin.com/",
      "prefill": "https://mohawkaustin.com/",
      "editor": "textfield"
    },
    "maxEvents": {
      "title": "Max Events",
      "type": "integer",
      "description": "Maximum number of events to collect (0 = unlimited)",
      "default": 200,
      "minimum": 0
    }
  },
  "required": ["startUrl"]
}
