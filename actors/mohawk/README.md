# Mohawk Austin Calendar Scraper

Scrapes upcoming events from https://mohawkaustin.com/ and pushes normalized items to the default dataset.

## Run locally

```sh
apify run
```

Optional input:
- `startUrl` (default `https://mohawkaustin.com/`)
- `maxEvents` (default 200)
